# Upsun configuration, generated using AI at: 2026-04-01T18:44:30-06:00
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  python-website:
    type: python:3.13  # Latest supported Python runtime

    hooks:
      build: |
        set -ex
        pip install -r requirements.txt

    web:
      commands:
        start: python manage.py runserver 0.0.0.0:$PORT

# Single application, so default route to it
routes:
  https://{default}/:
    type: upstream
    upstream: python-website:http